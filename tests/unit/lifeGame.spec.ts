import {
  createRow,
  createBoard,
  isNextCell,
  getNeighboursLiveState,
  getNewLiveState
} from "../hooks/useLifeGame";

describe("lifeGame", () => {
  it("row", () => {
    expect().toMatch();
  });

  it("board", () => {
    expect().toMatch();
  });

  it("isNextCell", () => {
    expect().toMatch();
  });

  it("getNeighboursLiveState", () => {
    expect().toMatch();
  });

  it("getNewLiveState", () => {
    expect().toMatch();
  });
});
